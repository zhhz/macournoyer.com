---
filter: haml
dirty:  true
---
- each_main_page_posts do |page|
  %div.article
    %div.date{:title => page.created_at.to_s}
      %span.day= page.created_at.strftime('%d')
      %span.month= page.created_at.strftime('%b')
    %h1!= link_to_page(page)
    .body!= preserve(render(page))
    .footer
      %a{:href => page.url + "#disqus_thread" } Post a comment
